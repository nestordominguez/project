<p id="notice"><%= notice %></p>

<p class="col-md-4">
  <strong>Name:</strong>
  <%= @category.name %>
</p>
<div class="col-sm-12">
	<%= link_to 'Edit', edit_category_path(@category), class: "col-md-1 links" %>
	<%= link_to 'Back', categories_path, class: "col-md-1 links" %>
</div>
